--- a/AVRescueAndroid/app/build.gradle
+++ b/AVRescueAndroid/app/build.gradle
@@ -6,8 +6,8 @@
         applicationId 'ru.avrescue.app'
         minSdk 26
         targetSdk 36
-        versionCode 40
-        versionName '2.3.11'
+        versionCode 320
+        versionName '3.20'
     }
 }
 dependencies {
--- a/AVRescueAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
+++ b/AVRescueAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
@@ -12,21 +12,23 @@
  void build(){root=new LinearLayout(this);root.setOrientation(LinearLayout.VERTICAL);root.setBackgroundColor(Ui.BG);ScrollView s=new ScrollView(this);content=Ui.column(this);s.addView(content);root.addView(s,new LinearLayout.LayoutParams(-1,0,1));nav=new LinearLayout(this);nav.setOrientation(LinearLayout.HORIZONTAL);nav.setBackgroundColor(Ui.PANEL);root.addView(nav,new LinearLayout.LayoutParams(-1,-2));setContentView(root);}
  void header(){content.addView(Ui.brand(this));content.addView(Ui.chip(this,"● САО • СВАО • ближайшая Московская область",true));}
  void load(){content.removeAllViews();header();content.addView(Ui.muted(this,"Загрузка…",14));new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->render(r));}catch(Exception e){runOnUiThread(()->error("Нет связи с сервером"));}}).start();}
- void refreshQuiet(){new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->{if(r.optBoolean("ok")){data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if("Главная".equals(currentPage))home();else if("Работа на линии".equals(currentPage))linePage();else if("Заказы".equals(currentPage)||"Заявки и заказы".equals(currentPage))orders();else if("Доход и комиссия".equals(currentPage))finance();}});}catch(Exception ignored){}}).start();}
+ void refreshQuiet(){new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->{if(r.optBoolean("ok")){data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if("Главная".equals(currentPage))home();else if("Работа на линии".equals(currentPage))linePage();else if("Заказы".equals(currentPage)||"Заявки и заказы".equals(currentPage))orders();else if("Доход и комиссия".equals(currentPage))finance();else if("Профиль".equals(currentPage))profile();}});}catch(Exception ignored){}}).start();}
  void error(String s){content.removeAllViews();header();content.addView(Ui.muted(this,s,15));}
- void render(JSONObject r){if(r.optInt("_http")==401){ApiClient.clear(this);startActivity(new Intent(this,LoginActivity.class));finish();return;}if(!r.optBoolean("ok")){error(r.optString("error","Ошибка"));return;}data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if(executor&&getIntent().getBooleanExtra("open_line",false)){getIntent().removeExtra("open_line");linePage();}else home();if(executor&&!polling){polling=true;handler.postDelayed(poll,3000);}}
+ void render(JSONObject r){if(r.optInt("_http")==401){ApiClient.clear(this);startActivity(new Intent(this,LoginActivity.class));finish();return;}if(!r.optBoolean("ok")){error(r.optString("error","Ошибка"));return;}data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if(executor&&getIntent().getBooleanExtra("open_line",false))getIntent().removeExtra("open_line");home();if(executor&&!polling){polling=true;handler.postDelayed(poll,3000);}}
  boolean staff(){return "superadmin".equals(role)||"admin".equals(role)||"dispatcher".equals(role)||"staff".equals(role);}boolean admin(){return "superadmin".equals(role)||"admin".equals(role);}
  void addNav(String t,Runnable r){Button b=new Button(this);b.setText(t);b.setAllCaps(false);b.setTextColor(Color.WHITE);b.setTextSize(10);b.setPadding(1,0,1,0);b.setBackgroundColor(Color.TRANSPARENT);b.setOnClickListener(v->r.run());nav.addView(b,new LinearLayout.LayoutParams(0,Ui.dp(this,54),1));}
- void setupNav(){nav.removeAllViews();if(staff()){addNav("Главная",()->home());addNav("Заказы",()->orders());addNav("Водители",()->resources("workers","Исполнители"));addNav("Автопарк",()->resources("fleet","Автопарк"));addNav("Ещё",()->more());}else if(executor){addNav("Главная",()->home());addNav("Заказы",()->orders());addNav(online?"На линии":"Не на линии",()->linePage());addNav("Доход",()->finance());addNav("Ещё",()->more());}else{addNav("Главная",()->home());addNav("Заказы",()->orders());addNav("Помощь",()->startActivity(new Intent(this,CreateOrderActivity.class)));addNav("Профиль",()->more());}}
+ void setupNav(){nav.removeAllViews();if(staff()){addNav("Главная",()->home());addNav("Заказы",()->orders());addNav("Водители",()->resources("workers","Исполнители"));addNav("Автопарк",()->resources("fleet","Автопарк"));addNav("Ещё",()->more());}else if(executor){addNav("Главная",()->home());addNav("Заказы",()->orders());addNav("Доход",()->finance());addNav("Профиль",()->profile());}else{addNav("Главная",()->home());addNav("Заказы",()->orders());addNav("Помощь",()->startActivity(new Intent(this,CreateOrderActivity.class)));addNav("Профиль",()->more());}}
  void page(String t){currentPage=t;handler.removeCallbacks(offerTicker);offerTimer=null;offerDeadlineElapsed=0;content.removeAllViews();header();content.addView(Ui.text(this,t,25,true));}
- void home(){page("Главная");JSONObject p=data.optJSONObject("profile"),ex=data.optJSONObject("executor");LinearLayout c=Ui.card(this);c.addView(Ui.text(this,"Здравствуйте, "+(p==null?"":p.optString("name")),22,true));c.addView(Ui.chip(this,"● "+roleRu(role),true));if(ex!=null){c.addView(Ui.muted(this,"Рейтинг "+ex.optDouble("rating",5)+" • комиссия "+Math.round(ex.optDouble("commission_rate",10))+"%",14));c.addView(Ui.chip(this,online?"● ВЫ НА ЛИНИИ":"● НЕ НА ЛИНИИ",online));}content.addView(c);JSONObject st=data.optJSONObject("stats");if(staff()&&st!=null){LinearLayout s=Ui.card(this);s.addView(Ui.text(this,"Сводка",19,true));s.addView(Ui.text(this,"Новые: "+st.optInt("new")+"   Активные: "+st.optInt("active"),16,true));s.addView(Ui.text(this,"Завершено: "+st.optInt("done")+"   Отменено: "+st.optInt("cancelled"),15,false));content.addView(s);}if(staff()){quick("Заявки и заказы","Диспетчерская",()->orders());quick("Исполнители","Статусы, рейтинг, геопозиция",()->resources("workers","Исполнители"));if(admin())quick("Заявки на регистрацию","Документы и автоматические проверки",()->applications());}else if(executor){driverOffer();partnerOrdersSection(2);quick("Работа на линии",online?"GPS передаётся • система ищет заказы рядом":"Выйдите на линию, чтобы получать ближайшие заказы",()->linePage());quick("Мои заказы","Предложения и активные заказы",()->orders());quick("Доход и комиссия","Баланс, комиссия AV Rescue и к выплате",()->finance());}else{quick("Вызвать помощь","Рассчитать стоимость и создать заказ",()->startActivity(new Intent(this,CreateOrderActivity.class)));quick("Мои заказы","Отслеживание, стоимость и рейтинг",()->orders());}preview();}
- void driverOffer(){JSONArray a=data.optJSONArray("orders");if(a==null)return;for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o!=null&&"offered".equals(o.optString("status"))){String id=o.optString("id");notifiedOfferId=id;LinearLayout c=Ui.card(this);c.addView(Ui.text(this,"НОВЫЙ ЗАКАЗ",21,true));c.addView(Ui.text(this,o.optString("service","Услуга"),18,true));c.addView(Ui.muted(this,"Подача: "+o.optString("address","Не указана"),14));c.addView(Ui.muted(this,"Назначение: "+o.optString("destination","Не указано"),14));double nearby=o.optDouble("distance_to_pickup_km",-1);if(nearby>=0)c.addView(Ui.chip(this,"До подачи ≈ "+nearby+" км по прямой",true));double price=o.optDouble("estimated_price",0);double rate=data.optJSONObject("executor")==null?10:data.optJSONObject("executor").optDouble("commission_rate",10);if(price>0){c.addView(Ui.text(this,Math.round(price)+" ₽",22,true));c.addView(Ui.muted(this,"Комиссия "+Math.round(rate)+"% • вам ≈ "+Math.round(price*(1-rate/100.0))+" ₽",14));}int eta=o.optInt("estimated_arrival_min",0);if(eta>0)c.addView(Ui.chip(this,"До клиента ≈ "+eta+" мин",true));int seconds=Math.max(0,Math.min(300,o.optInt("offer_seconds_left",300)));offerDeadlineElapsed=SystemClock.elapsedRealtime()+seconds*1000L;offerTimer=Ui.chip(this,"На принятие: "+offerTimeLeft(),true);c.addView(offerTimer);Button accept=Ui.button(this,"Принять заказ");accept.setOnClickListener(v->acceptAndOpen(id,accept));c.addView(accept);content.addView(c);handler.removeCallbacks(offerTicker);handler.post(offerTicker);break;}}}
+ void home(){page("Главная");JSONObject p=data.optJSONObject("profile"),ex=data.optJSONObject("executor");LinearLayout c=Ui.card(this);c.addView(Ui.text(this,(p==null?"Исполнитель":p.optString("name","Исполнитель")),22,true));if(ex!=null)c.addView(Ui.muted(this,"★ "+oneDecimal(ex.optDouble("rating",5))+"   •   комиссия "+Math.round(ex.optDouble("commission_rate",10))+"%",14));content.addView(c);JSONObject st=data.optJSONObject("stats");if(staff()&&st!=null){LinearLayout s=Ui.card(this);s.addView(Ui.text(this,"Сводка",19,true));s.addView(Ui.text(this,"Новые: "+st.optInt("new")+"   Активные: "+st.optInt("active"),16,true));s.addView(Ui.text(this,"Завершено: "+st.optInt("done")+"   Отменено: "+st.optInt("cancelled"),15,false));content.addView(s);}if(staff()){quick("Заявки и заказы","Диспетчерская",()->orders());quick("Исполнители","Статусы, рейтинг, геопозиция",()->resources("workers","Исполнители"));if(admin())quick("Заявки на регистрацию","Документы и автоматические проверки",()->applications());}else if(executor){driverHomeStats();Button status=online?Ui.darkButton(this,"● НА ЛИНИИ — НАЖАТЬ, ЧТОБЫ УЙТИ"):Ui.button(this,"ВЫЙТИ НА ЛИНИЮ");status.setOnClickListener(v->{if(online)goOffline();else startLocationUpdates();});content.addView(status);driverOffer();partnerOrdersSection(2);activeOrderPreview();}else{quick("Вызвать помощь","Рассчитать стоимость и создать заказ",()->startActivity(new Intent(this,CreateOrderActivity.class)));quick("Мои заказы","Отслеживание, стоимость и рейтинг",()->orders());preview();}}
+ void driverHomeStats(){JSONObject ui=data.optJSONObject("ui"),h=ui==null?null:ui.optJSONObject("home"),ex=data.optJSONObject("executor"),f=data.optJSONObject("finance"),summary=data.optJSONObject("stats");double rating=h==null?(ex==null?5:ex.optDouble("rating",5)):h.optDouble("rating",5);int completed=h==null?(summary==null?0:summary.optInt("done")):h.optInt("completed_orders");double available=h==null?(f==null?0:f.optDouble("available",0)):h.optDouble("available_balance",0);double today=h==null?0:h.optDouble("today_earnings",0);LinearLayout stats=Ui.card(this);stats.addView(Ui.text(this,"★ "+oneDecimal(rating)+"     "+completed+" заказов",18,true));stats.addView(Ui.text(this,"Доступно "+Math.round(available)+" ₽",21,true));stats.addView(Ui.muted(this,"Сегодня заработано: "+Math.round(today)+" ₽",14));content.addView(stats);}
+ void activeOrderPreview(){JSONArray a=data.optJSONArray("orders");if(a==null)return;for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o==null)continue;String s=o.optString("status");if(!Arrays.asList("assigned","enroute","arrived","loading","in_transit").contains(s))continue;LinearLayout c=Ui.card(this);c.addView(Ui.text(this,"Активный заказ",19,true));order(c,o);content.addView(c);return;}}
+ void driverOffer(){JSONArray a=data.optJSONArray("orders");if(a==null)return;for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o!=null&&"offered".equals(o.optString("status"))){String id=o.optString("id");notifiedOfferId=id;LinearLayout c=Ui.card(this);c.addView(Ui.text(this,"НОВЫЙ ЗАКАЗ",21,true));c.addView(Ui.text(this,o.optString("service","Услуга"),18,true));c.addView(Ui.muted(this,"Точка А: "+o.optString("address","Не указана"),14));double nearby=o.optDouble("distance_to_pickup_km",-1);if(nearby>=0)c.addView(Ui.chip(this,"До подачи ≈ "+nearby+" км",true));JSONObject terms=o.optJSONObject("offer_financials");double price=terms==null?o.optDouble("estimated_price",0):terms.optDouble("price",0);double rate=terms==null?(data.optJSONObject("executor")==null?10:data.optJSONObject("executor").optDouble("commission_rate",10)):terms.optDouble("commission_rate",0);double net=terms==null?price*(1-rate/100.0):terms.optDouble("driver_net",0);if(price>0){c.addView(Ui.text(this,Math.round(price)+" ₽",22,true));c.addView(Ui.muted(this,"Комиссия "+Math.round(rate)+"% • вам "+Math.round(net)+" ₽",14));}c.addView(Ui.muted(this,"Точка Б, телефон и комментарий откроются после принятия.",12));int eta=o.optInt("estimated_arrival_min",0);if(eta>0)c.addView(Ui.chip(this,"До клиента ≈ "+eta+" мин",true));int seconds=Math.max(0,Math.min(300,o.optInt("offer_seconds_left",300)));offerDeadlineElapsed=SystemClock.elapsedRealtime()+seconds*1000L;offerTimer=Ui.chip(this,"На принятие: "+offerTimeLeft(),true);c.addView(offerTimer);Button accept=Ui.button(this,"Принять заказ");accept.setOnClickListener(v->acceptAndOpen(id,accept));c.addView(accept);content.addView(c);handler.removeCallbacks(offerTicker);handler.post(offerTicker);break;}}}
  String offerTimeLeft(){long millis=Math.max(0,offerDeadlineElapsed-SystemClock.elapsedRealtime());long left=(millis+999)/1000;return String.format(java.util.Locale.US,"%d:%02d",left/60,left%60);}
  void acceptAndOpen(String id,Button button){stopOfferAlert();button.setEnabled(false);button.setText("Принимаем заказ…");new Thread(()->{try{JSONObject r=ApiClient.post("/api/mobile_order_action.php",new JSONObject().put("order_id",id).put("action","accept"));runOnUiThread(()->{if(r.optBoolean("ok")&&"assigned".equals(r.optString("status"))){handler.removeCallbacks(offerTicker);openOrder(id);}else{button.setEnabled(true);button.setText("Принять заказ");Toast.makeText(this,r.optString("error","Заказ уже недоступен"),Toast.LENGTH_LONG).show();refreshQuiet();}});}catch(Exception e){runOnUiThread(()->{button.setEnabled(true);button.setText("Принять заказ");Toast.makeText(this,"Нет связи с AV Rescue",Toast.LENGTH_LONG).show();});}}).start();}
  void quick(String t,String d,Runnable r){LinearLayout c=Ui.card(this);c.addView(Ui.text(this,t,18,true));c.addView(Ui.muted(this,d,13));c.setOnClickListener(v->r.run());content.addView(c);}
  void preview(){LinearLayout c=Ui.card(this);c.addView(Ui.text(this,staff()?"Последние заказы":"Мои заказы",19,true));JSONArray a=data.optJSONArray("orders");if(a==null||a.length()==0)c.addView(Ui.muted(this,"Заказов пока нет.",14));else for(int i=0;i<Math.min(3,a.length());i++)order(c,a.optJSONObject(i));content.addView(c);}
  void orders(){page(staff()?"Заявки и заказы":"Заказы");if(executor)partnerOrdersSection(0);JSONArray a=data.optJSONArray("orders");if(executor)content.addView(Ui.text(this,"Заказы AV Rescue",19,true));if(a==null||a.length()==0)content.addView(Ui.muted(this,"Заказов AV Rescue пока нет.",14));else for(int i=0;i<a.length();i++){LinearLayout c=Ui.card(this);order(c,a.optJSONObject(i));content.addView(c);}Button b=Ui.darkButton(this,"Обновить");b.setOnClickListener(v->refreshQuiet());content.addView(b);}
- void order(LinearLayout p,JSONObject o){if(o==null)return;p.addView(Ui.text(this,o.optString("id")+" • "+o.optString("service"),15,true));p.addView(Ui.muted(this,statusRu(o.optString("status"))+" • "+o.optString("address"),13));double nearby=o.optDouble("distance_to_pickup_km",-1);if(executor&&nearby>=0)p.addView(Ui.chip(this,"До подачи ≈ "+nearby+" км по прямой",true));double price=o.optDouble("final_price",o.optDouble("estimated_price",0));if(price>0)p.addView(Ui.text(this,Math.round(price)+" ₽",16,true));String id=o.optString("id");p.setOnClickListener(v->openOrder(id));}
+ void order(LinearLayout p,JSONObject o){if(o==null)return;String status=o.optString("status"),created=o.optString("created_at","");p.addView(Ui.text(this,o.optString("id")+" • "+o.optString("service"),15,true));p.addView(Ui.chip(this,statusRu(status),"done".equals(status)||"completed".equals(status)||Arrays.asList("assigned","enroute","arrived","loading","in_transit").contains(status)));if(!created.isEmpty())p.addView(Ui.muted(this,created.replace('T',' ').replace("+03:00","").replace("Z",""),12));p.addView(Ui.muted(this,"А: "+o.optString("address","Не указана"),13));if(!o.optBoolean("details_locked",false)&&!o.optString("destination","").isEmpty())p.addView(Ui.muted(this,"Б: "+o.optString("destination"),13));double nearby=o.optDouble("distance_to_pickup_km",-1);if(executor&&nearby>=0)p.addView(Ui.chip(this,"До подачи ≈ "+nearby+" км",true));JSONObject terms=o.optJSONObject("offer_financials");double price=terms==null?o.optDouble("final_price",o.optDouble("estimated_price",0)):terms.optDouble("price",0);if(price>0){p.addView(Ui.text(this,"Стоимость "+Math.round(price)+" ₽",16,true));if(executor&&terms!=null)p.addView(Ui.muted(this,"Комиссия "+Math.round(terms.optDouble("commission"))+" ₽ • вам "+Math.round(terms.optDouble("driver_net"))+" ₽",13));}String id=o.optString("id");p.setOnClickListener(v->openOrder(id));}
  void openOrder(String id){Intent in=new Intent(this,OrderDetailActivity.class);in.putExtra("order_id",id);in.putExtra("role",role);startActivity(in);}
  void partnerOrdersSection(int limit){
   JSONArray rows=data.optJSONArray("partner_orders");
@@ -113,6 +115,8 @@
  }
  void requestPayout(EditText amountField,EditText detailsField,Button button){double amount;try{amount=Double.parseDouble(amountField.getText().toString().trim().replace(',','.'));}catch(Exception e){Toast.makeText(this,"Укажите сумму выплаты",Toast.LENGTH_LONG).show();return;}String details=detailsField.getText().toString().trim();if(details.length()<4){Toast.makeText(this,"Укажите телефон СБП или реквизиты",Toast.LENGTH_LONG).show();return;}button.setEnabled(false);button.setText("Отправляем…");new Thread(()->{try{JSONObject r=ApiClient.post("/api/mobile_payout.php",new JSONObject().put("amount",amount).put("details",details));runOnUiThread(()->{if(r.optBoolean("ok")){Toast.makeText(this,"Заявка "+r.optString("payout_id")+" создана",Toast.LENGTH_LONG).show();refreshQuiet();}else{button.setEnabled(true);button.setText("Отправить заявку");Toast.makeText(this,r.optString("error","Заявка не создана"),Toast.LENGTH_LONG).show();}});}catch(Exception e){runOnUiThread(()->{button.setEnabled(true);button.setText("Отправить заявку");Toast.makeText(this,"Нет связи с AV Rescue",Toast.LENGTH_LONG).show();});}}).start();}
  String payoutStatus(String status){if("new".equals(status))return "● На рассмотрении";if("approved".equals(status))return "● Одобрена";if("paid".equals(status))return "● Выплачена";if("rejected".equals(status))return "● Отклонена";return "● "+status;}
+ void profile(){page("Профиль");JSONObject p=data.optJSONObject("profile"),ex=data.optJSONObject("executor");LinearLayout identity=Ui.card(this);identity.addView(Ui.text(this,p==null?"Исполнитель":p.optString("name","Исполнитель"),22,true));if(p!=null&&!p.optString("phone","").isEmpty())identity.addView(Ui.muted(this,p.optString("phone"),14));if(ex!=null){identity.addView(Ui.text(this,"★ "+oneDecimal(ex.optDouble("rating",5)),24,true));identity.addView(Ui.muted(this,"Комиссия "+Math.round(ex.optDouble("commission_rate",10))+"% • вам "+Math.round(ex.optDouble("payout_rate",90))+"%",14));}content.addView(identity);if(ex!=null){JSONObject quality=ex.optJSONObject("quality"),rating=ex.optJSONObject("rating_profile");LinearLayout stats=Ui.card(this);stats.addView(Ui.text(this,"Рейтинг и качество",19,true));if(rating!=null){int count=rating.optInt("ratings_count",rating.optInt("count",0));if(count>0)stats.addView(Ui.muted(this,"Оценок учтено: "+count,14));double recent=rating.optDouble("recent_average",rating.optDouble("weighted",0));if(recent>0)stats.addView(Ui.muted(this,"Средняя оценка: "+oneDecimal(recent),14));}if(quality!=null){stats.addView(Ui.muted(this,"Выполнено: "+quality.optInt("completed_orders")+" • принято: "+quality.optInt("accepted_orders"),14));if(quality.has("acceptance_rate"))stats.addView(Ui.muted(this,"Принятие заказов: "+Math.round(quality.optDouble("acceptance_rate"))+"%",14));if(quality.has("cancellation_rate"))stats.addView(Ui.muted(this,"Отмены: "+Math.round(quality.optDouble("cancellation_rate"))+"%",14));}stats.addView(Ui.muted(this,"Рейтинг считается по последним 150 оценкам: свежие влияют сильнее.",12));content.addView(stats);}quick("Данные и документы","Фото, автомобиль, ВУ и СТС",()->section("Данные и документы","Данные исполнителя хранятся в общей базе AV Rescue."));quick("Выполняемые услуги","Выберите категории заказов",()->servicesPage());quick("Поддержка","8-925-584-58-58",()->{});Button out=Ui.darkButton(this,"Выйти из аккаунта");out.setOnClickListener(v->logout());content.addView(out);}
+ String oneDecimal(double value){return String.format(java.util.Locale.US,"%.1f",value);}
  void more(){page("Ещё");if(staff()){if(admin()){quick("Заявки на регистрацию","Анкеты, фото, ВУ, СТС",()->applications());quick("Пользователи / клиенты","Просмотр, блокировка и удаление",()->resources("users","Пользователи / клиенты"));quick("B2B-партнёры","Корпоративные аккаунты",()->resources("b2b","B2B-партнёры"));quick("Сотрудники","Администраторы и диспетчеры",()->resources("staff","Сотрудники"));}quick("Тарифы и финансы","Комиссии, цены, выплаты",()->section("Тарифы и финансы","Управление тарифами подключено к общей серверной модели."));}else if(executor){quick("Профиль исполнителя","Фото, автомобиль и документы",()->section("Профиль исполнителя","Данные исполнителя хранятся в общей базе AV Rescue."));quick("Выполняемые услуги","Выберите категории заказов",()->servicesPage());quick("Поддержка","8-925-584-58-58",()->{});}else quick("Профиль клиента","История и данные аккаунта",()->{});Button out=Ui.darkButton(this,"Выйти из аккаунта");out.setOnClickListener(v->logout());content.addView(out);}
  void applications(){page("Заявки на регистрацию");new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_registration_admin.php");runOnUiThread(()->{page("Заявки на регистрацию");JSONArray a=r.optJSONArray("applications");if(a==null||a.length()==0){content.addView(Ui.muted(this,"Заявок пока нет.",14));return;}for(int i=0;i<a.length();i++){JSONObject x=a.optJSONObject(i);LinearLayout c=Ui.card(this);c.addView(Ui.text(this,x.optString("name","Исполнитель"),17,true));c.addView(Ui.muted(this,x.optString("vehicle_type")+" • "+x.optString("plate"),13));c.addView(Ui.chip(this,x.optString("status","auto_approved"),true));c.addView(Ui.muted(this,"Администратор может провести выборочную проверку документов.",12));content.addView(c);}});}catch(Exception e){runOnUiThread(()->error("Нет связи с сервером"));}}).start();}
  void resources(String key,String title){page(title);new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_resources.php");runOnUiThread(()->resourceRender(r,key,title));}catch(Exception e){runOnUiThread(()->error("Нет связи с сервером"));}}).start();}
--- a/AVRescueAndroid/app/src/main/java/ru/avrescue/app/OrderDetailActivity.java
+++ b/AVRescueAndroid/app/src/main/java/ru/avrescue/app/OrderDetailActivity.java
@@ -66,7 +66,9 @@
   summary.addView(Ui.chip(this,statusRu(order.optString("status")),true));
   summary.addView(Ui.text(this,order.optString("service","Услуга"),20,true));
   summary.addView(Ui.muted(this,"Адрес подачи: "+order.optString("address","Не указан"),14));
-  summary.addView(Ui.muted(this,"Адрес назначения: "+order.optString("destination","Не указан"),14));
+  boolean detailsLocked=order.optBoolean("details_locked",false);
+  if(detailsLocked)summary.addView(Ui.muted(this,"Точка Б, телефон и комментарий откроются после принятия.",13));
+  else summary.addView(Ui.muted(this,"Адрес назначения: "+order.optString("destination","Не указан"),14));
   String vehicle=order.optString("vehicle_type","");
   String plate=order.optString("license_plate",order.optString("car",""));
   String dimensions=order.optDouble("vehicle_weight_kg",0)>0?" • "+Math.round(order.optDouble("vehicle_weight_kg"))+" кг":"";
@@ -74,6 +76,9 @@
   if(!vehicle.isEmpty()||!plate.isEmpty())summary.addView(Ui.muted(this,(vehicle+" • "+plate+dimensions).trim(),14));
   double price=order.optDouble("final_price",order.optDouble("estimated_price",0));
   if(price>0)summary.addView(Ui.text(this,Math.round(price)+" ₽",22,true));
+  JSONObject offerFinancials=order.optJSONObject("offer_financials");
+  if("executor".equals(role)&&offerFinancials!=null)summary.addView(Ui.muted(this,"Комиссия "+Math.round(offerFinancials.optDouble("commission"))+" ₽ • вам "+Math.round(offerFinancials.optDouble("driver_net"))+" ₽",14));
+  if(!detailsLocked){String phone=order.optString("phone","");if(!phone.isEmpty()){Button call=Ui.darkButton(this,"Позвонить клиенту: "+phone);call.setOnClickListener(v->startActivity(new Intent(Intent.ACTION_DIAL,Uri.parse("tel:"+Uri.encode(phone)))));summary.addView(call);}String comment=order.optString("comment","");if(!comment.isEmpty())summary.addView(Ui.muted(this,"Комментарий: "+comment,13));}
   if(order.optBoolean("loading_complexity",false))summary.addView(Ui.chip(this,"Сложная погрузка учтена",true));
   if(order.optInt("dollies",0)>0)summary.addView(Ui.chip(this,"Подкаты: "+order.optInt("dollies"),true));
   if(order.optBoolean("dead_battery",false))summary.addView(Ui.chip(this,"Севший АКБ учтён",true));
