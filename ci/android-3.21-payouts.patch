diff --git a/AVRescueAndroid/app/build.gradle b/AVRescueAndroid/app/build.gradle
index a4a1721..fac4489 100644
--- a/AVRescueAndroid/app/build.gradle
+++ b/AVRescueAndroid/app/build.gradle
@@ -6,8 +6,8 @@ android {
         applicationId 'ru.avrescue.app'
         minSdk 26
         targetSdk 36
-        versionCode 320
-        versionName '3.20'
+        versionCode 321
+        versionName '3.21'
     }
 }
 dependencies {
diff --git a/AVRescueAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java b/AVRescueAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
index dfa85ce..0be7e3c 100644
--- a/AVRescueAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
+++ b/AVRescueAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
@@ -10,7 +10,7 @@ public class DashboardActivity extends Activity implements LocationListener{
  protected void onResume(){super.onResume();if(executor&&!polling){polling=true;handler.postDelayed(poll,3000);}}
  protected void onPause(){super.onPause();handler.removeCallbacks(poll);handler.removeCallbacks(offerTicker);polling=false;}
  void build(){root=new LinearLayout(this);root.setOrientation(LinearLayout.VERTICAL);root.setBackgroundColor(Ui.BG);ScrollView s=new ScrollView(this);content=Ui.column(this);s.addView(content);root.addView(s,new LinearLayout.LayoutParams(-1,0,1));nav=new LinearLayout(this);nav.setOrientation(LinearLayout.HORIZONTAL);nav.setBackgroundColor(Ui.PANEL);root.addView(nav,new LinearLayout.LayoutParams(-1,-2));setContentView(root);}
- void header(){content.addView(Ui.brand(this));content.addView(Ui.chip(this,"● САО • СВАО • ближайшая Московская область",true));}
+ void header(){content.addView(Ui.brand(this));content.addView(Ui.chip(this,"● Москва • Московская область • межгород",true));}
  void load(){content.removeAllViews();header();content.addView(Ui.muted(this,"Загрузка…",14));new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->render(r));}catch(Exception e){runOnUiThread(()->error("Нет связи с сервером"));}}).start();}
  void refreshQuiet(){new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->{if(r.optBoolean("ok")){data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if("Главная".equals(currentPage))home();else if("Работа на линии".equals(currentPage))linePage();else if("Заказы".equals(currentPage)||"Заявки и заказы".equals(currentPage))orders();else if("Доход и комиссия".equals(currentPage))finance();else if("Профиль".equals(currentPage))profile();}});}catch(Exception ignored){}}).start();}
  void error(String s){content.removeAllViews();header();content.addView(Ui.muted(this,s,15));}
@@ -109,12 +109,15 @@ public class DashboardActivity extends Activity implements LocationListener{
    c.addView(Ui.muted(this,"Заказы: "+Math.round(f.optDouble("gross"))+" ₽",14));c.addView(Ui.muted(this,"Ваш заработок: "+Math.round(f.optDouble("earned"))+" ₽",14));c.addView(Ui.muted(this,"Комиссия AV Rescue: "+Math.round(f.optDouble("commission"))+" ₽",14));
   }
   if(ex!=null)c.addView(Ui.muted(this,"Текущая ставка комиссии: "+Math.round(ex.optDouble("commission_rate",10))+"%",14));content.addView(c);
-  if(f!=null&&f.optDouble("available",0)>0){LinearLayout payout=Ui.card(this);payout.addView(Ui.text(this,"Заказать выплату",19,true));payout.addView(Ui.muted(this,"Укажите сумму и телефон СБП или реквизиты счёта.",13));EditText amount=Ui.input(this,"Сумма, ₽");amount.setInputType(android.text.InputType.TYPE_CLASS_NUMBER|android.text.InputType.TYPE_NUMBER_FLAG_DECIMAL);EditText details=Ui.input(this,"Телефон СБП или реквизиты");payout.addView(amount);payout.addView(details);Button send=Ui.button(this,"Отправить заявку");send.setOnClickListener(v->requestPayout(amount,details,send));payout.addView(send);content.addView(payout);}
+  JSONObject account=data.optJSONObject("payout_account");LinearLayout accountCard=Ui.card(this);accountCard.addView(Ui.text(this,"Куда получать деньги",19,true));boolean accountReady=account!=null&&account.optBoolean("ready",account.optBoolean("is_ready",false));accountCard.addView(Ui.muted(this,accountReady?account.optString("method_label","Способ выплаты")+" • "+account.optString("account_mask",account.optString("masked","")):"Способ выплаты ещё не настроен",14));Button editAccount=Ui.darkButton(this,accountReady?"Изменить реквизиты":"Настроить способ выплаты");editAccount.setOnClickListener(v->showPayoutAccountDialog());accountCard.addView(editAccount);content.addView(accountCard);
+  if(f!=null){double available=f.optDouble("available",0);if(available>=4000){LinearLayout payout=Ui.card(this);payout.addView(Ui.text(this,"Заказать выплату",19,true));payout.addView(Ui.muted(this,"Введите сумму. Деньги придут на сохранённые реквизиты в ближайший платёжный день — вторник или пятницу.",13));EditText amount=Ui.input(this,"Сумма, ₽ (от 4 000)");amount.setInputType(android.text.InputType.TYPE_CLASS_NUMBER|android.text.InputType.TYPE_NUMBER_FLAG_DECIMAL);payout.addView(amount);Button send=Ui.button(this,"Запросить выплату");send.setEnabled(accountReady);send.setOnClickListener(v->requestPayout(amount,send));payout.addView(send);if(!accountReady)payout.addView(Ui.muted(this,"Сначала сохраните способ выплаты.",12));content.addView(payout);}else if(available>0){LinearLayout minimum=Ui.card(this);minimum.addView(Ui.text(this,"До выплаты осталось "+Math.round(4000-available)+" ₽",17,true));minimum.addView(Ui.muted(this,"Минимальная сумма выплаты — 4 000 ₽.",13));content.addView(minimum);}}
   if(f!=null){JSONArray payouts=f.optJSONArray("payouts");LinearLayout payoutHistory=Ui.card(this);payoutHistory.addView(Ui.text(this,"Выплаты",19,true));if(payouts==null||payouts.length()==0)payoutHistory.addView(Ui.muted(this,"Заявок на выплату пока нет.",14));else for(int i=0;i<payouts.length();i++){JSONObject x=payouts.optJSONObject(i);if(x==null)continue;payoutHistory.addView(Ui.text(this,x.optString("id","Выплата")+" • "+Math.round(x.optDouble("amount"))+" ₽",15,true));payoutHistory.addView(Ui.chip(this,payoutStatus(x.optString("status")),"paid".equals(x.optString("status"))));}content.addView(payoutHistory);}
   if(f!=null){JSONArray tx=f.optJSONArray("transactions");LinearLayout history=Ui.card(this);history.addView(Ui.text(this,"История по заказам",19,true));if(tx==null||tx.length()==0)history.addView(Ui.muted(this,"Операций пока нет.",14));else for(int i=0;i<tx.length();i++){JSONObject x=tx.optJSONObject(i);if(x==null)continue;boolean cash="cash".equals(x.optString("payment_method"));history.addView(Ui.text(this,x.optString("order_id","Заказ")+" • "+Math.round(x.optDouble("price"))+" ₽",16,true));history.addView(Ui.muted(this,(cash?"Наличные":"Оплата картой")+" • комиссия "+Math.round(x.optDouble("commission"))+" ₽",13));double change=x.optDouble("balance_change");history.addView(Ui.muted(this,(change>=0?"Зачислено: +":"Списано: ")+Math.round(Math.abs(change))+" ₽",13));}content.addView(history);}
  }
- void requestPayout(EditText amountField,EditText detailsField,Button button){double amount;try{amount=Double.parseDouble(amountField.getText().toString().trim().replace(',','.'));}catch(Exception e){Toast.makeText(this,"Укажите сумму выплаты",Toast.LENGTH_LONG).show();return;}String details=detailsField.getText().toString().trim();if(details.length()<4){Toast.makeText(this,"Укажите телефон СБП или реквизиты",Toast.LENGTH_LONG).show();return;}button.setEnabled(false);button.setText("Отправляем…");new Thread(()->{try{JSONObject r=ApiClient.post("/api/mobile_payout.php",new JSONObject().put("amount",amount).put("details",details));runOnUiThread(()->{if(r.optBoolean("ok")){Toast.makeText(this,"Заявка "+r.optString("payout_id")+" создана",Toast.LENGTH_LONG).show();refreshQuiet();}else{button.setEnabled(true);button.setText("Отправить заявку");Toast.makeText(this,r.optString("error","Заявка не создана"),Toast.LENGTH_LONG).show();}});}catch(Exception e){runOnUiThread(()->{button.setEnabled(true);button.setText("Отправить заявку");Toast.makeText(this,"Нет связи с AV Rescue",Toast.LENGTH_LONG).show();});}}).start();}
- String payoutStatus(String status){if("new".equals(status))return "● На рассмотрении";if("approved".equals(status))return "● Одобрена";if("paid".equals(status))return "● Выплачена";if("rejected".equals(status))return "● Отклонена";return "● "+status;}
+ void showPayoutAccountDialog(){LinearLayout form=Ui.column(this);Spinner method=new Spinner(this);String[] methods={"СБП","Банковская карта","Расчётный счёт"};method.setAdapter(new ArrayAdapter<String>(this,android.R.layout.simple_spinner_dropdown_item,methods));EditText holder=Ui.input(this,"Получатель — ФИО"),primary=Ui.input(this,"Телефон СБП"),bik=Ui.input(this,"БИК — только для счёта"),bank=Ui.input(this,"Название банка");form.addView(method);form.addView(holder);form.addView(primary);form.addView(bik);form.addView(bank);method.setOnItemSelectedListener(new android.widget.AdapterView.OnItemSelectedListener(){public void onNothingSelected(android.widget.AdapterView<?> p){}public void onItemSelected(android.widget.AdapterView<?> p,View v,int x,long id){primary.setHint(x==0?"Телефон СБП":x==1?"Номер банковской карты":"Номер расчётного счёта (20 цифр)");bik.setVisibility(x==2?View.VISIBLE:View.GONE);}});JSONObject p=data.optJSONObject("profile"),old=data.optJSONObject("payout_account");holder.setText(old==null?(p==null?"":p.optString("name","")):old.optString("holder_name",p==null?"":p.optString("name","")));bank.setText(old==null?"":old.optString("bank_name",""));new AlertDialog.Builder(this).setTitle("Способ выплаты").setView(form).setNegativeButton("Отмена",null).setPositiveButton("Сохранить",(d,w)->savePayoutAccount(method.getSelectedItemPosition(),holder.getText().toString(),primary.getText().toString(),bik.getText().toString(),bank.getText().toString())).show();}
+ void savePayoutAccount(int selected,String holder,String value,String bik,String bank){String method=selected==0?"sbp":selected==1?"card":"bank_account";JSONObject body=new JSONObject();try{body.put("method",method).put("holder_name",holder).put("bank_name",bank);if("sbp".equals(method))body.put("phone",value);else if("card".equals(method))body.put("card_number",value);else body.put("bank_account",value).put("bik",bik);}catch(Exception ignored){}new Thread(()->{try{JSONObject r=ApiClient.post("/api/payout_account.php",body);runOnUiThread(()->{Toast.makeText(this,r.optBoolean("ok")?"Способ выплаты сохранён":r.optString("error","Не удалось сохранить"),Toast.LENGTH_LONG).show();if(r.optBoolean("ok"))load();});}catch(Exception e){runOnUiThread(()->Toast.makeText(this,"Нет связи с AV Rescue",Toast.LENGTH_LONG).show());}}).start();}
+ void requestPayout(EditText amountField,Button button){double amount;try{amount=Double.parseDouble(amountField.getText().toString().trim().replace(',','.'));}catch(Exception e){Toast.makeText(this,"Укажите сумму выплаты",Toast.LENGTH_LONG).show();return;}double available=data.optJSONObject("finance")==null?0:data.optJSONObject("finance").optDouble("available",0);if(amount<4000){Toast.makeText(this,"Минимальная сумма выплаты — 4 000 ₽",Toast.LENGTH_LONG).show();return;}if(amount>available){Toast.makeText(this,"Недостаточно доступных средств",Toast.LENGTH_LONG).show();return;}button.setEnabled(false);button.setText("Отправляем…");new Thread(()->{try{JSONObject r=ApiClient.post("/api/mobile_payout.php",new JSONObject().put("amount",amount));runOnUiThread(()->{if(r.optBoolean("ok")){Toast.makeText(this,"Заявка "+r.optString("payout_id")+" создана",Toast.LENGTH_LONG).show();refreshQuiet();}else{button.setEnabled(true);button.setText("Запросить выплату");Toast.makeText(this,r.optString("error","Заявка не создана"),Toast.LENGTH_LONG).show();}});}catch(Exception e){runOnUiThread(()->{button.setEnabled(true);button.setText("Запросить выплату");Toast.makeText(this,"Нет связи с AV Rescue",Toast.LENGTH_LONG).show();});}}).start();}
+ String payoutStatus(String status){if("new".equals(status))return "● На рассмотрении";if("approved".equals(status))return "● Одобрена";if("processing".equals(status))return "● Передана в банк";if("paid".equals(status))return "● Выплачена";if("failed".equals(status))return "● Ошибка выплаты";if("rejected".equals(status))return "● Отклонена";return "● "+status;}
  void profile(){page("Профиль");JSONObject p=data.optJSONObject("profile"),ex=data.optJSONObject("executor");LinearLayout identity=Ui.card(this);identity.addView(Ui.text(this,p==null?"Исполнитель":p.optString("name","Исполнитель"),22,true));if(p!=null&&!p.optString("phone","").isEmpty())identity.addView(Ui.muted(this,p.optString("phone"),14));if(ex!=null){identity.addView(Ui.text(this,"★ "+oneDecimal(ex.optDouble("rating",5)),24,true));identity.addView(Ui.muted(this,"Комиссия "+Math.round(ex.optDouble("commission_rate",10))+"% • вам "+Math.round(ex.optDouble("payout_rate",90))+"%",14));}content.addView(identity);if(ex!=null){JSONObject quality=ex.optJSONObject("quality"),rating=ex.optJSONObject("rating_profile");LinearLayout stats=Ui.card(this);stats.addView(Ui.text(this,"Рейтинг и качество",19,true));if(rating!=null){int count=rating.optInt("ratings_count",rating.optInt("count",0));if(count>0)stats.addView(Ui.muted(this,"Оценок учтено: "+count,14));double recent=rating.optDouble("recent_average",rating.optDouble("weighted",0));if(recent>0)stats.addView(Ui.muted(this,"Средняя оценка: "+oneDecimal(recent),14));}if(quality!=null){stats.addView(Ui.muted(this,"Выполнено: "+quality.optInt("completed_orders")+" • принято: "+quality.optInt("accepted_orders"),14));if(quality.has("acceptance_rate"))stats.addView(Ui.muted(this,"Принятие заказов: "+Math.round(quality.optDouble("acceptance_rate"))+"%",14));if(quality.has("cancellation_rate"))stats.addView(Ui.muted(this,"Отмены: "+Math.round(quality.optDouble("cancellation_rate"))+"%",14));}stats.addView(Ui.muted(this,"Рейтинг считается по последним 150 оценкам: свежие влияют сильнее.",12));content.addView(stats);}quick("Данные и документы","Фото, автомобиль, ВУ и СТС",()->section("Данные и документы","Данные исполнителя хранятся в общей базе AV Rescue."));quick("Выполняемые услуги","Выберите категории заказов",()->servicesPage());quick("Поддержка","8-925-584-58-58",()->{});Button out=Ui.darkButton(this,"Выйти из аккаунта");out.setOnClickListener(v->logout());content.addView(out);}
  String oneDecimal(double value){return String.format(java.util.Locale.US,"%.1f",value);}
  void more(){page("Ещё");if(staff()){if(admin()){quick("Заявки на регистрацию","Анкеты, фото, ВУ, СТС",()->applications());quick("Пользователи / клиенты","Просмотр, блокировка и удаление",()->resources("users","Пользователи / клиенты"));quick("B2B-партнёры","Корпоративные аккаунты",()->resources("b2b","B2B-партнёры"));quick("Сотрудники","Администраторы и диспетчеры",()->resources("staff","Сотрудники"));}quick("Тарифы и финансы","Комиссии, цены, выплаты",()->section("Тарифы и финансы","Управление тарифами подключено к общей серверной модели."));}else if(executor){quick("Профиль исполнителя","Фото, автомобиль и документы",()->section("Профиль исполнителя","Данные исполнителя хранятся в общей базе AV Rescue."));quick("Выполняемые услуги","Выберите категории заказов",()->servicesPage());quick("Поддержка","8-925-584-58-58",()->{});}else quick("Профиль клиента","История и данные аккаунта",()->{});Button out=Ui.darkButton(this,"Выйти из аккаунта");out.setOnClickListener(v->logout());content.addView(out);}
diff --git a/AVRescueAndroid/app/src/main/java/ru/avrescue/app/CreateOrderActivity.java b/AVRescueAndroid/app/src/main/java/ru/avrescue/app/CreateOrderActivity.java
index 50c099e..872a9bf 100644
--- a/AVRescueAndroid/app/src/main/java/ru/avrescue/app/CreateOrderActivity.java
+++ b/AVRescueAndroid/app/src/main/java/ru/avrescue/app/CreateOrderActivity.java
@@ -2,7 +2,7 @@ package ru.avrescue.app;
 import android.app.*;import android.os.*;import android.content.*;import android.content.pm.PackageManager;import android.location.*;import android.widget.*;import org.json.JSONObject;import java.util.*;
 public class CreateOrderActivity extends Activity implements LocationListener{
  Spinner service,vehicle,payment;EditText address,destination,licensePlate,vehicleWeight,vehicleLength,distance,comment;TextView geo,status,price;LocationManager lm;double lat=0,lng=0;long estimate=0;
- public void onCreate(Bundle b){super.onCreate(b);LinearLayout l=Ui.column(this);ScrollView s=new ScrollView(this);s.addView(l);setContentView(s);l.addView(Ui.brand(this));l.addView(Ui.text(this,"Вызвать помощь",26,true));l.addView(Ui.chip(this,"● САО • СВАО • ближайшая Московская область",true));LinearLayout c=Ui.card(this);service=spin(new String[]{"Эвакуация","Техпомощь на дороге","Запуск двигателя","Замена колеса","Подвоз топлива","Вскрытие автомобиля"});vehicle=spin(new String[]{"Легковой автомобиль","Внедорожник","Коммерческий транспорт","Мотоцикл","Грузовой автомобиль","Спецтехника"});payment=spin(new String[]{"Наличные","Онлайн — после подключения Альфа-Банка"});address=Ui.input(this,"Адрес подачи");destination=Ui.input(this,"Куда везём / место назначения");licensePlate=Ui.input(this,"Государственный регистрационный номер");vehicleWeight=Ui.input(this,"Масса автомобиля, кг");vehicleLength=Ui.input(this,"Длина автомобиля, м");distance=Ui.input(this,"Расстояние по маршруту, км (для теста)");comment=Ui.input(this,"Комментарий: колёса, блокировка, низкий клиренс и т.д.");geo=Ui.muted(this,"Геопозиция не определена",13);price=Ui.text(this,"Предварительная стоимость: —",20,true);status=Ui.muted(this,"",14);c.addView(Ui.muted(this,"Услуга",12));c.addView(service);c.addView(Ui.muted(this,"Автомобиль",12));c.addView(vehicle);c.addView(address);c.addView(destination);c.addView(licensePlate);c.addView(vehicleWeight);c.addView(vehicleLength);c.addView(distance);c.addView(comment);c.addView(Ui.muted(this,"Оплата",12));c.addView(payment);c.addView(geo);Button gps=Ui.darkButton(this,"Определить геопозицию");gps.setOnClickListener(v->gps());c.addView(gps);Button calc=Ui.darkButton(this,"Рассчитать стоимость");calc.setOnClickListener(v->calc());c.addView(calc);c.addView(price);Button send=Ui.button(this,"Подтвердить и вызвать помощь");send.setOnClickListener(v->send());c.addView(send);c.addView(status);l.addView(c);vehicle.setOnItemSelectedListener(new AdapterView.OnItemSelectedListener(){public void onItemSelected(AdapterView<?> p,android.view.View v,int pos,long id){updateVehicleFields();}public void onNothingSelected(AdapterView<?> p){}});updateVehicleFields();}
+ public void onCreate(Bundle b){super.onCreate(b);LinearLayout l=Ui.column(this);ScrollView s=new ScrollView(this);s.addView(l);setContentView(s);l.addView(Ui.brand(this));l.addView(Ui.text(this,"Вызвать помощь",26,true));l.addView(Ui.chip(this,"● Москва • Московская область • межгород",true));LinearLayout c=Ui.card(this);service=spin(new String[]{"Эвакуация","Техпомощь на дороге","Запуск двигателя","Замена колеса","Подвоз топлива","Вскрытие автомобиля"});vehicle=spin(new String[]{"Легковой автомобиль","Внедорожник","Коммерческий транспорт","Мотоцикл","Грузовой автомобиль","Спецтехника"});payment=spin(new String[]{"Наличные","Онлайн — после подключения Альфа-Банка"});address=Ui.input(this,"Адрес подачи");destination=Ui.input(this,"Куда везём / место назначения");licensePlate=Ui.input(this,"Государственный регистрационный номер");vehicleWeight=Ui.input(this,"Масса автомобиля, кг");vehicleLength=Ui.input(this,"Длина автомобиля, м");distance=Ui.input(this,"Расстояние по маршруту, км (для теста)");comment=Ui.input(this,"Комментарий: колёса, блокировка, низкий клиренс и т.д.");geo=Ui.muted(this,"Геопозиция не определена",13);price=Ui.text(this,"Предварительная стоимость: —",20,true);status=Ui.muted(this,"",14);c.addView(Ui.muted(this,"Услуга",12));c.addView(service);c.addView(Ui.muted(this,"Автомобиль",12));c.addView(vehicle);c.addView(address);c.addView(destination);c.addView(licensePlate);c.addView(vehicleWeight);c.addView(vehicleLength);c.addView(distance);c.addView(comment);c.addView(Ui.muted(this,"Оплата",12));c.addView(payment);c.addView(geo);Button gps=Ui.darkButton(this,"Определить геопозицию");gps.setOnClickListener(v->gps());c.addView(gps);Button calc=Ui.darkButton(this,"Рассчитать стоимость");calc.setOnClickListener(v->calc());c.addView(calc);c.addView(price);Button send=Ui.button(this,"Подтвердить и вызвать помощь");send.setOnClickListener(v->send());c.addView(send);c.addView(status);l.addView(c);vehicle.setOnItemSelectedListener(new AdapterView.OnItemSelectedListener(){public void onItemSelected(AdapterView<?> p,android.view.View v,int pos,long id){updateVehicleFields();}public void onNothingSelected(AdapterView<?> p){}});updateVehicleFields();}
  Spinner spin(String[] a){Spinner s=new Spinner(this);s.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,a));return s;}double km(){try{return Double.parseDouble(distance.getText().toString().replace(',','.'));}catch(Exception e){return 0;}}
  double number(EditText field){try{return Double.parseDouble(field.getText().toString().replace(',','.'));}catch(Exception e){return 0;}}
  void updateVehicleFields(){String type=vehicle.getSelectedItem().toString();boolean passenger=type.contains("Легковой"),commercial=type.contains("Коммерческий");vehicleWeight.setVisibility(passenger||commercial?android.view.View.VISIBLE:android.view.View.GONE);vehicleLength.setVisibility(commercial?android.view.View.VISIBLE:android.view.View.GONE);}
diff --git a/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java b/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
index 9964f5a..f5855e6 100644
--- a/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
+++ b/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
@@ -10,7 +10,7 @@ public class DashboardActivity extends Activity implements LocationListener{
  protected void onResume(){super.onResume();if(data!=null&&!staff()&&!polling){polling=true;handler.postDelayed(poll,3000);}}
  protected void onPause(){super.onPause();handler.removeCallbacks(poll);handler.removeCallbacks(offerTicker);polling=false;}
  void build(){root=new LinearLayout(this);root.setOrientation(LinearLayout.VERTICAL);root.setBackgroundColor(Ui.BG);ScrollView s=new ScrollView(this);content=Ui.column(this);s.addView(content);root.addView(s,new LinearLayout.LayoutParams(-1,0,1));nav=new LinearLayout(this);nav.setOrientation(LinearLayout.HORIZONTAL);nav.setBackgroundColor(Ui.PANEL);root.addView(nav,new LinearLayout.LayoutParams(-1,-2));setContentView(root);}
- void header(){content.addView(Ui.brand(this));content.addView(Ui.chip(this,"● САО • СВАО • ближайшая Московская область",true));}
+ void header(){content.addView(Ui.brand(this));content.addView(Ui.chip(this,"● Москва • Московская область • межгород",true));}
  void load(){content.removeAllViews();header();content.addView(Ui.muted(this,"Загрузка…",14));new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->render(r));}catch(Exception e){runOnUiThread(()->error("Нет связи с сервером"));}}).start();}
  void refreshQuiet(){new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->{if(r.optBoolean("ok")){checkCancelledOrders(r,true);data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if("Главная".equals(currentPage))home();else if("Работа на линии".equals(currentPage))linePage();else if("Заказы".equals(currentPage)||"Заявки и заказы".equals(currentPage))orders();else if("Доход и комиссия".equals(currentPage))finance();}});}catch(Exception ignored){}}).start();}
  void error(String s){content.removeAllViews();header();content.addView(Ui.muted(this,s,15));}
diff --git a/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/CreateOrderActivity.java b/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/CreateOrderActivity.java
index 50c099e..872a9bf 100644
--- a/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/CreateOrderActivity.java
+++ b/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/CreateOrderActivity.java
@@ -2,7 +2,7 @@ package ru.avrescue.app;
 import android.app.*;import android.os.*;import android.content.*;import android.content.pm.PackageManager;import android.location.*;import android.widget.*;import org.json.JSONObject;import java.util.*;
 public class CreateOrderActivity extends Activity implements LocationListener{
  Spinner service,vehicle,payment;EditText address,destination,licensePlate,vehicleWeight,vehicleLength,distance,comment;TextView geo,status,price;LocationManager lm;double lat=0,lng=0;long estimate=0;
- public void onCreate(Bundle b){super.onCreate(b);LinearLayout l=Ui.column(this);ScrollView s=new ScrollView(this);s.addView(l);setContentView(s);l.addView(Ui.brand(this));l.addView(Ui.text(this,"Вызвать помощь",26,true));l.addView(Ui.chip(this,"● САО • СВАО • ближайшая Московская область",true));LinearLayout c=Ui.card(this);service=spin(new String[]{"Эвакуация","Техпомощь на дороге","Запуск двигателя","Замена колеса","Подвоз топлива","Вскрытие автомобиля"});vehicle=spin(new String[]{"Легковой автомобиль","Внедорожник","Коммерческий транспорт","Мотоцикл","Грузовой автомобиль","Спецтехника"});payment=spin(new String[]{"Наличные","Онлайн — после подключения Альфа-Банка"});address=Ui.input(this,"Адрес подачи");destination=Ui.input(this,"Куда везём / место назначения");licensePlate=Ui.input(this,"Государственный регистрационный номер");vehicleWeight=Ui.input(this,"Масса автомобиля, кг");vehicleLength=Ui.input(this,"Длина автомобиля, м");distance=Ui.input(this,"Расстояние по маршруту, км (для теста)");comment=Ui.input(this,"Комментарий: колёса, блокировка, низкий клиренс и т.д.");geo=Ui.muted(this,"Геопозиция не определена",13);price=Ui.text(this,"Предварительная стоимость: —",20,true);status=Ui.muted(this,"",14);c.addView(Ui.muted(this,"Услуга",12));c.addView(service);c.addView(Ui.muted(this,"Автомобиль",12));c.addView(vehicle);c.addView(address);c.addView(destination);c.addView(licensePlate);c.addView(vehicleWeight);c.addView(vehicleLength);c.addView(distance);c.addView(comment);c.addView(Ui.muted(this,"Оплата",12));c.addView(payment);c.addView(geo);Button gps=Ui.darkButton(this,"Определить геопозицию");gps.setOnClickListener(v->gps());c.addView(gps);Button calc=Ui.darkButton(this,"Рассчитать стоимость");calc.setOnClickListener(v->calc());c.addView(calc);c.addView(price);Button send=Ui.button(this,"Подтвердить и вызвать помощь");send.setOnClickListener(v->send());c.addView(send);c.addView(status);l.addView(c);vehicle.setOnItemSelectedListener(new AdapterView.OnItemSelectedListener(){public void onItemSelected(AdapterView<?> p,android.view.View v,int pos,long id){updateVehicleFields();}public void onNothingSelected(AdapterView<?> p){}});updateVehicleFields();}
+ public void onCreate(Bundle b){super.onCreate(b);LinearLayout l=Ui.column(this);ScrollView s=new ScrollView(this);s.addView(l);setContentView(s);l.addView(Ui.brand(this));l.addView(Ui.text(this,"Вызвать помощь",26,true));l.addView(Ui.chip(this,"● Москва • Московская область • межгород",true));LinearLayout c=Ui.card(this);service=spin(new String[]{"Эвакуация","Техпомощь на дороге","Запуск двигателя","Замена колеса","Подвоз топлива","Вскрытие автомобиля"});vehicle=spin(new String[]{"Легковой автомобиль","Внедорожник","Коммерческий транспорт","Мотоцикл","Грузовой автомобиль","Спецтехника"});payment=spin(new String[]{"Наличные","Онлайн — после подключения Альфа-Банка"});address=Ui.input(this,"Адрес подачи");destination=Ui.input(this,"Куда везём / место назначения");licensePlate=Ui.input(this,"Государственный регистрационный номер");vehicleWeight=Ui.input(this,"Масса автомобиля, кг");vehicleLength=Ui.input(this,"Длина автомобиля, м");distance=Ui.input(this,"Расстояние по маршруту, км (для теста)");comment=Ui.input(this,"Комментарий: колёса, блокировка, низкий клиренс и т.д.");geo=Ui.muted(this,"Геопозиция не определена",13);price=Ui.text(this,"Предварительная стоимость: —",20,true);status=Ui.muted(this,"",14);c.addView(Ui.muted(this,"Услуга",12));c.addView(service);c.addView(Ui.muted(this,"Автомобиль",12));c.addView(vehicle);c.addView(address);c.addView(destination);c.addView(licensePlate);c.addView(vehicleWeight);c.addView(vehicleLength);c.addView(distance);c.addView(comment);c.addView(Ui.muted(this,"Оплата",12));c.addView(payment);c.addView(geo);Button gps=Ui.darkButton(this,"Определить геопозицию");gps.setOnClickListener(v->gps());c.addView(gps);Button calc=Ui.darkButton(this,"Рассчитать стоимость");calc.setOnClickListener(v->calc());c.addView(calc);c.addView(price);Button send=Ui.button(this,"Подтвердить и вызвать помощь");send.setOnClickListener(v->send());c.addView(send);c.addView(status);l.addView(c);vehicle.setOnItemSelectedListener(new AdapterView.OnItemSelectedListener(){public void onItemSelected(AdapterView<?> p,android.view.View v,int pos,long id){updateVehicleFields();}public void onNothingSelected(AdapterView<?> p){}});updateVehicleFields();}
  Spinner spin(String[] a){Spinner s=new Spinner(this);s.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,a));return s;}double km(){try{return Double.parseDouble(distance.getText().toString().replace(',','.'));}catch(Exception e){return 0;}}
  double number(EditText field){try{return Double.parseDouble(field.getText().toString().replace(',','.'));}catch(Exception e){return 0;}}
  void updateVehicleFields(){String type=vehicle.getSelectedItem().toString();boolean passenger=type.contains("Легковой"),commercial=type.contains("Коммерческий");vehicleWeight.setVisibility(passenger||commercial?android.view.View.VISIBLE:android.view.View.GONE);vehicleLength.setVisibility(commercial?android.view.View.VISIBLE:android.view.View.GONE);}
