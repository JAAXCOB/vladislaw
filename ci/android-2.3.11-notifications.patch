--- a/AVRescueAndroid/app/src/main/java/ru/avrescue/app/DriverLocationService.java
+++ b/AVRescueAndroid/app/src/main/java/ru/avrescue/app/DriverLocationService.java
@@ -9,6 +9,7 @@
 import android.net.Uri;
 import android.os.*;
 import org.json.*;
+import java.util.*;
 
 public class DriverLocationService extends Service implements LocationListener {
  public static final String ACTION_START="ru.avrescue.app.START_DRIVER_SERVICE";
@@ -16,17 +17,18 @@
  public static final String ACTION_STOP_ALERT="ru.avrescue.app.STOP_ORDER_ALERT";
  private static final String CHANNEL_TRACKING="driver_tracking";
  private static final String CHANNEL_ORDERS="new_orders_v2";
+ private static final String CHANNEL_CANCELLATIONS="order_cancellations";
  private static final long MAX_LOCATION_AGE_MS=120000;
  private static final float MAX_LOCATION_ACCURACY_M=250f;
  private final Handler handler=new Handler(Looper.getMainLooper());
- private LocationManager locationManager;private volatile Location lastLocation;private volatile long lastHeartbeatElapsed=0;private String alertedOfferId="";private Ringtone ringtone;
+ private LocationManager locationManager;private volatile Location lastLocation;private volatile long lastHeartbeatElapsed=0;private String alertedOfferId="";private Ringtone ringtone;private final java.util.concurrent.ConcurrentHashMap<String,String> knownOrderStates=new java.util.concurrent.ConcurrentHashMap<>();private volatile boolean orderStatesInitialized=false;
  private final Runnable poll=new Runnable(){public void run(){pollOrders();handler.postDelayed(this,8000);}};
 
  public void onCreate(){super.onCreate();ApiClient.load(this);createChannels();startForeground(4101,trackingNotification());startLocation();handler.post(poll);}
  public int onStartCommand(Intent intent,int flags,int startId){String action=intent==null?ACTION_START:intent.getAction();if(ACTION_STOP_ALERT.equals(action)){stopAlert();return START_STICKY;}if(ACTION_STOP.equals(action)){stopAlert();stopSelf();return START_NOT_STICKY;}return START_STICKY;}
  public android.os.IBinder onBind(Intent intent){return null;}
 
- private void createChannels(){NotificationManager nm=(NotificationManager)getSystemService(NOTIFICATION_SERVICE);if(Build.VERSION.SDK_INT>=26){NotificationChannel tracking=new NotificationChannel(CHANNEL_TRACKING,"Работа на линии",NotificationManager.IMPORTANCE_LOW);tracking.setSound(null,null);nm.createNotificationChannel(tracking);NotificationChannel orders=new NotificationChannel(CHANNEL_ORDERS,"Новые заказы",NotificationManager.IMPORTANCE_HIGH);orders.enableVibration(true);nm.createNotificationChannel(orders);}}
+ private void createChannels(){NotificationManager nm=(NotificationManager)getSystemService(NOTIFICATION_SERVICE);if(Build.VERSION.SDK_INT>=26){NotificationChannel tracking=new NotificationChannel(CHANNEL_TRACKING,"Работа на линии",NotificationManager.IMPORTANCE_LOW);tracking.setSound(null,null);nm.createNotificationChannel(tracking);NotificationChannel orders=new NotificationChannel(CHANNEL_ORDERS,"Новые заказы",NotificationManager.IMPORTANCE_HIGH);orders.enableVibration(true);nm.createNotificationChannel(orders);NotificationChannel cancellations=new NotificationChannel(CHANNEL_CANCELLATIONS,"Отмена заказов",NotificationManager.IMPORTANCE_HIGH);cancellations.enableVibration(true);nm.createNotificationChannel(cancellations);}}
  private Notification trackingNotification(){Intent open=new Intent(this,DashboardActivity.class).putExtra("open_line",true);PendingIntent pi=PendingIntent.getActivity(this,4101,open,PendingIntent.FLAG_UPDATE_CURRENT|PendingIntent.FLAG_IMMUTABLE);Notification.Builder b=Build.VERSION.SDK_INT>=26?new Notification.Builder(this,CHANNEL_TRACKING):new Notification.Builder(this);return b.setSmallIcon(android.R.drawable.ic_menu_mylocation).setContentTitle("AV Rescue — вы на линии").setContentText("Геопозиция и новые заказы обновляются в фоне").setOngoing(true).setContentIntent(pi).build();}
  private void startLocation(){locationManager=(LocationManager)getSystemService(LOCATION_SERVICE);if(Build.VERSION.SDK_INT>=23&&checkSelfPermission(android.Manifest.permission.ACCESS_FINE_LOCATION)!=PackageManager.PERMISSION_GRANTED){stopSelf();return;}try{if(locationManager.isProviderEnabled(LocationManager.GPS_PROVIDER))locationManager.requestLocationUpdates(LocationManager.GPS_PROVIDER,10000,0,this);if(locationManager.isProviderEnabled(LocationManager.NETWORK_PROVIDER))locationManager.requestLocationUpdates(LocationManager.NETWORK_PROVIDER,10000,0,this);Location gps=locationManager.getLastKnownLocation(LocationManager.GPS_PROVIDER),network=locationManager.getLastKnownLocation(LocationManager.NETWORK_PROVIDER);Location x=bestFreshLocation(gps,network);if(x!=null)sendLocation(x);}catch(Exception ignored){}}
  public void onLocationChanged(Location location){if(isFreshLocation(location))sendLocation(location);}
@@ -36,11 +38,12 @@
  private Location cachedFreshLocation(){android.content.SharedPreferences p=getSharedPreferences("avr",MODE_PRIVATE);long a=p.getLong("last_lat",0),b=p.getLong("last_lng",0),time=p.getLong("last_fix_time",0);if(a==0||b==0||time<=0||System.currentTimeMillis()-time>MAX_LOCATION_AGE_MS)return null;Location x=new Location("cached");x.setLatitude(Double.longBitsToDouble(a));x.setLongitude(Double.longBitsToDouble(b));x.setTime(time);float accuracy=p.getFloat("last_accuracy",0);if(accuracy>0)x.setAccuracy(accuracy);return isFreshLocation(x)?x:null;}
  private void sendLocation(Location location){if(!isFreshLocation(location))return;saveLocation(location);lastHeartbeatElapsed=SystemClock.elapsedRealtime();new Thread(()->{try{ApiClient.post("/api/mobile_location.php",new JSONObject().put("lat",location.getLatitude()).put("lng",location.getLongitude()).put("accuracy_m",location.hasAccuracy()?location.getAccuracy():JSONObject.NULL).put("captured_at_ms",location.getTime()).put("online",true));}catch(Exception ignored){}}).start();}
  private void heartbeat() throws Exception{long now=SystemClock.elapsedRealtime();if(now-lastHeartbeatElapsed<30000)return;Location current=isFreshLocation(lastLocation)?lastLocation:cachedFreshLocation();if(current==null)return;lastHeartbeatElapsed=now;ApiClient.post("/api/mobile_location.php",new JSONObject().put("lat",current.getLatitude()).put("lng",current.getLongitude()).put("accuracy_m",current.hasAccuracy()?current.getAccuracy():JSONObject.NULL).put("captured_at_ms",current.getTime()).put("online",true));}
- private void pollOrders(){new Thread(()->{try{heartbeat();JSONObject r=ApiClient.get("/api/mobile_dashboard.php");JSONArray orders=r.optJSONArray("orders");JSONObject offer=null;if(orders!=null)for(int i=0;i<orders.length();i++){JSONObject o=orders.optJSONObject(i);if(o!=null&&"offered".equals(o.optString("status"))){offer=o;break;}}JSONArray partner=r.optJSONArray("partner_orders");JSONObject newPartner=nearestUnnotifiedPartnerOrder(partner);final JSONObject current=offer,partnerOrder=newPartner;handler.post(()->{if(current==null){stopAlert();alertedOfferId="";}else if(!current.optString("id").equals(alertedOfferId)){alertedOfferId=current.optString("id");showOffer(current);}if(partnerOrder!=null)showPartnerOffer(partnerOrder);});}catch(Exception ignored){}}).start();}
+ private void pollOrders(){new Thread(()->{try{heartbeat();JSONObject r=ApiClient.get("/api/mobile_dashboard.php");JSONArray orders=r.optJSONArray("orders");JSONObject offer=null;ArrayList<JSONObject> cancelled=new ArrayList<>();if(orders!=null)for(int i=0;i<orders.length();i++){JSONObject o=orders.optJSONObject(i);if(o==null)continue;String id=o.optString("id",""),state=o.optString("status","");String previous=knownOrderStates.put(id,state);if(orderStatesInitialized&&previous!=null&&!"cancelled".equals(previous)&&"cancelled".equals(state))cancelled.add(o);if("offered".equals(state)&&offer==null)offer=o;}orderStatesInitialized=true;JSONArray partner=r.optJSONArray("partner_orders");JSONObject newPartner=nearestUnnotifiedPartnerOrder(partner);final JSONObject current=offer,partnerOrder=newPartner;final ArrayList<JSONObject> cancelledOrders=cancelled;handler.post(()->{if(current==null){stopAlert();alertedOfferId="";}else if(!current.optString("id").equals(alertedOfferId)){alertedOfferId=current.optString("id");showOffer(current);}if(partnerOrder!=null)showPartnerOffer(partnerOrder);for(JSONObject order:cancelledOrders)showCancellation(order);});}catch(Exception ignored){}}).start();}
  private JSONObject nearestUnnotifiedPartnerOrder(JSONArray partner){if(partner==null)return null;Location current=isFreshLocation(lastLocation)?lastLocation:cachedFreshLocation();JSONObject best=null;double bestDistance=Double.MAX_VALUE;for(int i=0;i<partner.length();i++){JSONObject order=partner.optJSONObject(i);if(order==null||!"available".equals(order.optString("status")))continue;String id=order.optString("id","");if(id.isEmpty()||getSharedPreferences("avr",MODE_PRIVATE).getBoolean("partner_notified_"+id,false))continue;double distance=distanceTo(order,current);if(best==null||distance<bestDistance){best=order;bestDistance=distance;}}return best;}
  private double distanceTo(JSONObject order,Location current){if(current==null)return order.optDouble("distance_km",Double.MAX_VALUE);double lat=order.optDouble("pickup_lat",0),lng=order.optDouble("pickup_lng",0);if(lat==0||lng==0)return Double.MAX_VALUE;float[] result=new float[1];Location.distanceBetween(current.getLatitude(),current.getLongitude(),lat,lng,result);return result[0];}
  private void showOffer(JSONObject order){stopAlert();try{Uri sound=RingtoneManager.getDefaultUri(RingtoneManager.TYPE_ALARM);if(sound==null)sound=RingtoneManager.getDefaultUri(RingtoneManager.TYPE_NOTIFICATION);ringtone=RingtoneManager.getRingtone(this,sound);if(Build.VERSION.SDK_INT>=28)ringtone.setLooping(true);ringtone.play();}catch(Exception ignored){}Intent open=new Intent(this,DashboardActivity.class).putExtra("open_line",true).addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP|Intent.FLAG_ACTIVITY_SINGLE_TOP);PendingIntent pi=PendingIntent.getActivity(this,order.optString("id").hashCode(),open,PendingIntent.FLAG_UPDATE_CURRENT|PendingIntent.FLAG_IMMUTABLE);Notification.Builder b=Build.VERSION.SDK_INT>=26?new Notification.Builder(this,CHANNEL_ORDERS):new Notification.Builder(this);b.setSmallIcon(android.R.drawable.ic_dialog_info).setContentTitle("Новый заказ AV Rescue").setContentText(order.optString("service")+" · "+order.optString("address")).setAutoCancel(true).setContentIntent(pi).setPriority(Notification.PRIORITY_MAX);((NotificationManager)getSystemService(NOTIFICATION_SERVICE)).notify(order.optString("id").hashCode(),b.build());}
  private void showPartnerOffer(JSONObject order){String id=order.optString("id","");if(id.isEmpty())return;getSharedPreferences("avr",MODE_PRIVATE).edit().putBoolean("partner_notified_"+id,true).apply();Intent open=new Intent(this,DashboardActivity.class).putExtra("open_line",true).addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP|Intent.FLAG_ACTIVITY_SINGLE_TOP);PendingIntent pi=PendingIntent.getActivity(this,("partner-"+id).hashCode(),open,PendingIntent.FLAG_UPDATE_CURRENT|PendingIntent.FLAG_IMMUTABLE);String route=order.optString("pickup_address","Координаты на карте");Notification.Builder b=Build.VERSION.SDK_INT>=26?new Notification.Builder(this,CHANNEL_ORDERS):new Notification.Builder(this);b.setSmallIcon(android.R.drawable.ic_dialog_info).setContentTitle("Новая заявка партнёра").setContentText(order.optString("service","Эвакуация")+" · "+route).setStyle(new Notification.BigTextStyle().bigText("Точка А: "+route+"\nОткройте заявку, чтобы увидеть точку Б и детали.")).setAutoCancel(true).setContentIntent(pi).setPriority(Notification.PRIORITY_HIGH);((NotificationManager)getSystemService(NOTIFICATION_SERVICE)).notify(("partner-"+id).hashCode(),b.build());}
+ private void showCancellation(JSONObject order){String id=order.optString("id","");if(id.isEmpty())return;stopAlert();String source="client".equals(order.optString("cancelled_by"))?"клиентом":"диспетчером";Intent open=new Intent(this,DashboardActivity.class).addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP|Intent.FLAG_ACTIVITY_SINGLE_TOP);PendingIntent pi=PendingIntent.getActivity(this,("cancel-"+id).hashCode(),open,PendingIntent.FLAG_UPDATE_CURRENT|PendingIntent.FLAG_IMMUTABLE);Notification.Builder b=Build.VERSION.SDK_INT>=26?new Notification.Builder(this,CHANNEL_CANCELLATIONS):new Notification.Builder(this);b.setSmallIcon(android.R.drawable.ic_dialog_alert).setContentTitle("Заказ отменён").setContentText("Заказ "+id+" отменён "+source).setAutoCancel(true).setContentIntent(pi).setPriority(Notification.PRIORITY_HIGH);((NotificationManager)getSystemService(NOTIFICATION_SERVICE)).notify(("cancel-"+id).hashCode(),b.build());}
  private void stopAlert(){try{if(ringtone!=null&&ringtone.isPlaying())ringtone.stop();}catch(Exception ignored){}ringtone=null;if(!alertedOfferId.isEmpty())try{((NotificationManager)getSystemService(NOTIFICATION_SERVICE)).cancel(alertedOfferId.hashCode());}catch(Exception ignored){}}
  public void onProviderEnabled(String provider){}public void onProviderDisabled(String provider){}public void onStatusChanged(String provider,int status,Bundle extras){}
  public void onDestroy(){handler.removeCallbacks(poll);stopAlert();try{if(locationManager!=null)locationManager.removeUpdates(this);}catch(Exception ignored){}super.onDestroy();}
--- a/AVRescueAndroid/app/build.gradle
+++ b/AVRescueAndroid/app/build.gradle
@@ -6,8 +6,8 @@
         applicationId 'ru.avrescue.app'
         minSdk 26
         targetSdk 36
-        versionCode 39
-        versionName '2.3.10'
+        versionCode 40
+        versionName '2.3.11'
     }
 }
 dependencies {
--- a/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
+++ b/AVRescueClientAndroid/app/src/main/java/ru/avrescue/app/DashboardActivity.java
@@ -1,19 +1,23 @@
 package ru.avrescue.app;
 import android.app.*;import android.os.*;import android.content.*;import android.content.pm.PackageManager;import android.graphics.Color;import android.location.*;import android.net.Uri;import android.view.*;import android.widget.*;import org.json.*;import java.time.*;
 public class DashboardActivity extends Activity implements LocationListener{
- LinearLayout root,content,nav;LocationManager lm;String role="client",notifiedOfferId="";JSONObject data;boolean executor=false,online=false;Location lastLocation;Handler handler=new Handler(Looper.getMainLooper());boolean polling=false,waitingForOnline=false;String currentPage="Главная";TextView offerTimer;long offerDeadlineElapsed=0;
- Runnable poll=new Runnable(){@Override public void run(){if(executor&&!isFinishing()){refreshQuiet();handler.postDelayed(this,10000);}}};
+ static final String CHANNEL_CANCELLATIONS="client_order_cancellations";
+ LinearLayout root,content,nav;LocationManager lm;String role="client",notifiedOfferId="";JSONObject data;boolean executor=false,online=false;Location lastLocation;Handler handler=new Handler(Looper.getMainLooper());boolean polling=false,waitingForOnline=false;String currentPage="Главная";TextView offerTimer;long offerDeadlineElapsed=0;java.util.HashMap<String,String> knownOrderStates=new java.util.HashMap<>();boolean orderStatesInitialized=false;
+ Runnable poll=new Runnable(){@Override public void run(){if(!staff()&&!isFinishing()){refreshQuiet();handler.postDelayed(this,10000);}}};
  Runnable offerTicker=new Runnable(){@Override public void run(){if(offerTimer==null||offerDeadlineElapsed<=0||isFinishing())return;String left=offerTimeLeft();offerTimer.setText("На принятие: "+left);if("0:00".equals(left)){refreshQuiet();return;}handler.postDelayed(this,1000);}};
- public void onCreate(Bundle b){super.onCreate(b);ApiClient.load(this);build();if(Build.VERSION.SDK_INT>=33&&checkSelfPermission(android.Manifest.permission.POST_NOTIFICATIONS)!=PackageManager.PERMISSION_GRANTED)requestPermissions(new String[]{android.Manifest.permission.POST_NOTIFICATIONS},92);load();}
+ public void onCreate(Bundle b){super.onCreate(b);ApiClient.load(this);createCancellationChannel();build();if(Build.VERSION.SDK_INT>=33&&checkSelfPermission(android.Manifest.permission.POST_NOTIFICATIONS)!=PackageManager.PERMISSION_GRANTED)requestPermissions(new String[]{android.Manifest.permission.POST_NOTIFICATIONS},92);load();}
  protected void onNewIntent(Intent intent){super.onNewIntent(intent);setIntent(intent);load();}
- protected void onResume(){super.onResume();if(executor&&!polling){polling=true;handler.postDelayed(poll,3000);}}
+ protected void onResume(){super.onResume();if(data!=null&&!staff()&&!polling){polling=true;handler.postDelayed(poll,3000);}}
  protected void onPause(){super.onPause();handler.removeCallbacks(poll);handler.removeCallbacks(offerTicker);polling=false;}
  void build(){root=new LinearLayout(this);root.setOrientation(LinearLayout.VERTICAL);root.setBackgroundColor(Ui.BG);ScrollView s=new ScrollView(this);content=Ui.column(this);s.addView(content);root.addView(s,new LinearLayout.LayoutParams(-1,0,1));nav=new LinearLayout(this);nav.setOrientation(LinearLayout.HORIZONTAL);nav.setBackgroundColor(Ui.PANEL);root.addView(nav,new LinearLayout.LayoutParams(-1,-2));setContentView(root);}
  void header(){content.addView(Ui.brand(this));content.addView(Ui.chip(this,"● САО • СВАО • ближайшая Московская область",true));}
  void load(){content.removeAllViews();header();content.addView(Ui.muted(this,"Загрузка…",14));new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->render(r));}catch(Exception e){runOnUiThread(()->error("Нет связи с сервером"));}}).start();}
- void refreshQuiet(){new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->{if(r.optBoolean("ok")){data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if("Главная".equals(currentPage))home();else if("Работа на линии".equals(currentPage))linePage();else if("Заказы".equals(currentPage)||"Заявки и заказы".equals(currentPage))orders();else if("Доход и комиссия".equals(currentPage))finance();}});}catch(Exception ignored){}}).start();}
+ void refreshQuiet(){new Thread(()->{try{JSONObject r=ApiClient.get("/api/mobile_dashboard.php");runOnUiThread(()->{if(r.optBoolean("ok")){checkCancelledOrders(r,true);data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if("Главная".equals(currentPage))home();else if("Работа на линии".equals(currentPage))linePage();else if("Заказы".equals(currentPage)||"Заявки и заказы".equals(currentPage))orders();else if("Доход и комиссия".equals(currentPage))finance();}});}catch(Exception ignored){}}).start();}
  void error(String s){content.removeAllViews();header();content.addView(Ui.muted(this,s,15));}
- void render(JSONObject r){if(r.optInt("_http")==401){ApiClient.clear(this);startActivity(new Intent(this,LoginActivity.class));finish();return;}if(!r.optBoolean("ok")){error(r.optString("error","Ошибка"));return;}data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if(executor&&getIntent().getBooleanExtra("open_line",false)){getIntent().removeExtra("open_line");linePage();}else home();if(executor&&!polling){polling=true;handler.postDelayed(poll,3000);}}
+ void render(JSONObject r){if(r.optInt("_http")==401){ApiClient.clear(this);startActivity(new Intent(this,LoginActivity.class));finish();return;}if(!r.optBoolean("ok")){error(r.optString("error","Ошибка"));return;}checkCancelledOrders(r,false);data=r;JSONObject p=r.optJSONObject("profile");role=p==null?"client":p.optString("role","client");executor="executor".equals(role);JSONObject ex=r.optJSONObject("executor");String executorStatus=ex==null?"":ex.optString("status");online=ex!=null&&("online".equals(executorStatus)||"reserved".equals(executorStatus)||"busy".equals(executorStatus));if(executor&&online)startDriverService();setupNav();if(executor&&getIntent().getBooleanExtra("open_line",false)){getIntent().removeExtra("open_line");linePage();}else home();if(!staff()&&!polling){polling=true;handler.postDelayed(poll,3000);}}
+ void createCancellationChannel(){if(Build.VERSION.SDK_INT>=26){NotificationChannel channel=new NotificationChannel(CHANNEL_CANCELLATIONS,"Отмена заказов",NotificationManager.IMPORTANCE_HIGH);channel.enableVibration(true);((NotificationManager)getSystemService(NOTIFICATION_SERVICE)).createNotificationChannel(channel);}}
+ void checkCancelledOrders(JSONObject response,boolean notify){JSONArray orders=response.optJSONArray("orders");if(orders==null)return;for(int i=0;i<orders.length();i++){JSONObject order=orders.optJSONObject(i);if(order==null)continue;String id=order.optString("id",""),state=order.optString("status","");String previous=knownOrderStates.put(id,state);if(notify&&orderStatesInitialized&&previous!=null&&!"cancelled".equals(previous)&&"cancelled".equals(state)&&!"client".equals(order.optString("cancelled_by")))showCancellation(order);}orderStatesInitialized=true;}
+ void showCancellation(JSONObject order){String id=order.optString("id","");Intent open=new Intent(this,OrderDetailActivity.class).putExtra("order_id",id).putExtra("role",role).addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP);PendingIntent pi=PendingIntent.getActivity(this,("client-cancel-"+id).hashCode(),open,PendingIntent.FLAG_UPDATE_CURRENT|PendingIntent.FLAG_IMMUTABLE);Notification.Builder b=Build.VERSION.SDK_INT>=26?new Notification.Builder(this,CHANNEL_CANCELLATIONS):new Notification.Builder(this);b.setSmallIcon(android.R.drawable.ic_dialog_alert).setContentTitle("Заказ отменён").setContentText("Заказ "+id+" отменён диспетчером").setAutoCancel(true).setContentIntent(pi).setPriority(Notification.PRIORITY_HIGH);((NotificationManager)getSystemService(NOTIFICATION_SERVICE)).notify(("client-cancel-"+id).hashCode(),b.build());}
  boolean staff(){return "superadmin".equals(role)||"admin".equals(role)||"dispatcher".equals(role)||"staff".equals(role);}boolean admin(){return "superadmin".equals(role)||"admin".equals(role);}
  void addNav(String t,Runnable r){Button b=new Button(this);b.setText(t);b.setAllCaps(false);b.setTextColor(Color.WHITE);b.setTextSize(10);b.setPadding(1,0,1,0);b.setBackgroundColor(Color.TRANSPARENT);b.setOnClickListener(v->r.run());nav.addView(b,new LinearLayout.LayoutParams(0,Ui.dp(this,54),1));}
  void setupNav(){nav.removeAllViews();if(staff()){addNav("Главная",()->home());addNav("Заказы",()->orders());addNav("Водители",()->resources("workers","Исполнители"));addNav("Автопарк",()->resources("fleet","Автопарк"));addNav("Ещё",()->more());}else if(executor){addNav("Главная",()->home());addNav("Заказы",()->orders());addNav(online?"На линии":"Не на линии",()->linePage());addNav("Доход",()->finance());addNav("Ещё",()->more());}else{addNav("Главная",()->home());addNav("Заказы",()->orders());addNav("Помощь",()->startActivity(new Intent(this,CreateOrderActivity.class)));addNav("Профиль",()->more());}}
--- a/AVRescueClientAndroid/app/build.gradle
+++ b/AVRescueClientAndroid/app/build.gradle
@@ -6,7 +6,7 @@
         applicationId 'ru.avrescue.client'
         minSdk 26
         targetSdk 36
-        versionCode 6
-        versionName '1.0.5'
+        versionCode 7
+        versionName '1.0.6'
     }
 }
